<%
    showRadio = (is_special) => {
        return(`
            <div class="form-group d-flex  justify-content-start">
                <label>Is Special</label>
                <div class="form-check ml-5">
                    <input class="form-check-input" type="radio" name="is_special" value="yes" ${is_special ? 'checked' : ''}>
                    <label class="form-check-label">Yes</label>
                </div>
                <div class="form-check  ml-5">
                    <input class="form-check-input" type="radio" name="is_special" value="no"  ${is_special ? '' : 'checked'}>
                    <label class="form-check-label">No</label>
                </div>
                
            </div>
        `)
    }
%>