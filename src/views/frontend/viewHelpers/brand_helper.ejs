<%
    showBrand = (brands) => {
        return brands.map((brand) => {
            return(`
                <li class="grid-item">
                  <a href="/shop?brand_id=${brand.id}"><img src="/uploads/brand/${brand.image}" alt="Clients"></a>
                </li>
            `)
        })
    }
%>