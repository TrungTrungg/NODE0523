<%
    handleOrdering = (collection, ordering, id) => {
        return(`
            <td id="status-${id}">
                <input 
                    onChange="javascript:handleOrderingChange('${collection}','${id}')"
                    type="number" 
                    value="${ordering}" 
                    class="ordering"    
                />
            </td>
        `)
    }

%>
