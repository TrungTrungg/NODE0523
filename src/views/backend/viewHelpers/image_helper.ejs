<%
    handleImage = (image) => {
        return image ? `<img class="img-thumbnail img-md" src="/backend/uploads/${image}" alt="" id="show-image">` : 
                        `<img class="img-thumbnail img-md" src="/backend/uploads/no-image.png" alt="" id="show-image">`
    }

%>
